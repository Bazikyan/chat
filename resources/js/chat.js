import React from 'react';
import ReactDom from 'react-dom';
import ChatContainer from './components/ChatContainer';

let root = document.getElementById('root');

ReactDom.render(<ChatContainer/>, root)
